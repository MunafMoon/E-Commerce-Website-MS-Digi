import { useQuery } from "@tanstack/react-query";
import { Link } from "react-router-dom";
import { api, Product, money } from "../lib/api";
export function AdminProducts(){ const {data=[]}=useQuery({queryKey:["admin-products"],queryFn:()=>api<Product[]>("/admin/products")}); return <div><div className="flex items-center justify-between"><h1 className="text-3xl font-semibold">Products</h1><Link to="/admin/products/new" className="rounded-lg bg-ink px-4 py-2 text-white">Add product</Link></div><div className="mt-6 overflow-hidden rounded-lg bg-white shadow-soft"><table className="w-full text-left text-sm"><thead className="bg-stone-50"><tr><th className="p-4">Product</th><th>Price</th><th>Stock</th><th>Status</th></tr></thead><tbody>{data.map(p=><tr className="border-t" key={p.id}><td className="p-4 font-medium">{p.name}</td><td>{money(p.salePrice ?? p.regularPrice)}</td><td>{p.stock}</td><td>{p.stock<=5?'Low stock':'Active'}</td></tr>)}</tbody></table></div></div> }
