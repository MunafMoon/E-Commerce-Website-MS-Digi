import { NavLink, Outlet, Navigate } from "react-router-dom";
import { BarChart3, Boxes, FileText, Home, LogOut, Settings, ShoppingCart, Tags, Users } from "lucide-react";
const items=[['Dashboard','/admin',BarChart3],['Products','/admin/products',Boxes],['Categories','/admin/categories',Tags],['Orders','/admin/orders',ShoppingCart],['Customers','/admin/customers',Users],['Homepage','/admin/homepage',Home],['Pages','/admin/pages',FileText],['Settings','/admin/settings',Settings]] as const;
export function AdminLayout(){ const role=localStorage.getItem('role'); if(role!=="ADMIN") return <Navigate to="/login"/>; return <main className="min-h-screen bg-stone-100 text-ink md:flex"><aside className="bg-ink p-4 text-white md:min-h-screen md:w-72"><h1 className="px-3 text-xl font-semibold">Commerce Admin</h1><nav className="mt-6 grid gap-1">{items.map(([label,to,Icon])=><NavLink end={to==='/admin'} key={to} to={to} className={({isActive})=>`flex items-center gap-3 rounded-lg px-3 py-2 text-sm ${isActive?'bg-white text-ink':'text-white/75 hover:bg-white/10'}`}><Icon size={18}/>{label}</NavLink>)}<button onClick={()=>localStorage.clear()} className="mt-4 flex items-center gap-3 rounded-lg px-3 py-2 text-sm text-white/75"><LogOut size={18}/>Logout</button></nav></aside><section className="flex-1 p-4 md:p-8"><Outlet/></section></main> }
